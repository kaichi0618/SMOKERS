<% @shops.each do |shop| %>
  <%= attachment_image_tag(shop, :image, :fill, 100, 100, fallback: "no-image-icon.jpg") %>
  <%= shop.shop_name %>
  <%= shop.address %>
  <%= link_to "詳細", shop_path(shop.id) %>
<% end %>